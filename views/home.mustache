<!DOCTYPE html>

<html>
    <head>
        <meta charset="utf-8" />

        <meta http-equiv="X-UA-Compatible" content="IE=edge" />

        <title>User</title>

        <meta name="viewport" content="width=device-width, initial-scale=1" />

        <link rel="stylesheet" type="text/css" href="" />
    </head>

    <body>
        <header>
            <h1>Home Page</h1>
            {{username}}
        </header>

        <main>
            <form action="/post-blog" method="POST">
                <label class="">Blog:</label>

                <input class="" type="text" name="author" placeholder="Author" />

                <input class="" type="text" name="title" placeholder="Title" />

                <input class="" type="text" name="content" placeholder="Content" />

                <input type="submit" value="Post Blog" />
            </form>
            
            <h2>All blogs</h2>
            <section>
            {{#blogs}}
            <div>
                    <input type="hidden" name="author" value="{{author}}" />
                    <div>{{author}}</div>

                    <input type="hidden" name="title" value="{{title}}" />
                    <div>{{title}}</div>

                    <input type="hidden" name="content" value="{{content}}" />
                    <div>{{content}}</div>

                    <a href="/modify-blog/{{blogid}}">Modify</a>
                <form action="/remove-blog" method="POST">
                <input type="hidden" name="blogid" value="{{blogid}}" />
                <input type="submit" value="Remove" />
                </form>
            </div>
            {{/blogs}}
            </section>
        </main>

        <footer>
        </footer>
    </body>

    <script src=""></script>
</html>